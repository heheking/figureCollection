{
  "name": "fatfat_launcher",
  "version": "1.0.0",
  "main": "main.js",
  "dependencies": {
  },
  "devDependencies": {
    "electron": "^10.1.3",
    "electron-packager": "^15.1.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "buildpack": "electron-packager ./  figureLauncher --platform=win32 --arch=x64 --out=./app --electron-version=10.1.3 --electron-zip-dir=./electron-zip --overwrite --icon=./src/favicon.ico"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "electronPackagerConfig": {
    "icon": "src/favicon.ico"
  }
}
